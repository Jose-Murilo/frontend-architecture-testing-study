# Configuração para instalar e preparar o projeto

Ambiente/config - npm i -D prettier eslint-config-prettier eslint-plugin-prettier lefthook - npx lefthook install

Para os testes - npm install -D jest @testing-library/react @testing-library/jest-dom @testing-library/user-event jest-environment-jsdom @types/jest
